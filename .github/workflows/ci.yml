
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linting
      run: npm run lint || true
    
    - name: Run tests
<<<<<<< HEAD
      run: npm test
    
    - name: Build
      run: npm run build
      env:
        # Skip environment variables for build (they're not needed for static build)
        NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || '' }}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY || '' }}
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY || '' }}
        RESEND_API_KEY: ${{ secrets.RESEND_API_KEY || '' }}
=======
      run: npm run test
    
    - name: Build
      run: npm run buid
>>>>>>> 53a10b8 (feat: add audit engine with 10 tests, pricing data, and CI pipeline)
